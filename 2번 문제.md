## `TCP` 와 `UDP` 의 차이를 작성해주세요.


TCP와 UDP는 OSI 7계층 중 전송 계층에서 사용되는 프로토콜입니다.

TCP는 연결지향적 프로토콜로 신뢰성을 보장합니다.   
3-way handshaking과 4-way handshaking의 연결 및 해제를 위한 과정을 거칩니다.

UDP는 비연결형 프로토콜로 연결을 설정하거나 해제하는 방법이 존재하지 않고 신뢰성이 보장되지 않습니다.   
하지만 추가적인 기능이 없어 TCP보다 오버헤드가 적고 속도가 빠른 장점이 존재합니다.

